{
  "name": "combustivel-ziran",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-gobrax-data.mjs",
    "cf:build": "node scripts/prepare-pages.mjs",
    "cf:project:create": "npx wrangler pages project create desempenho-gobrax",
    "cf:dev": "npm run cf:build && npx wrangler pages dev dist",
    "cf:deploy": "npm run sync-data && npm run cf:build && npx wrangler pages deploy dist --project-name=desempenho-gobrax"
  }
}
